# Secrets — never commit real API keys
.env
intake/.env

# Gathered output from intake/gather.mjs (photos, scraped text, filled INPUT)
intake-output/

# Node
node_modules/
npm-debug.log*
.DS_Store
*.log
