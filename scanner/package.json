{
  "name": "cadru-scanner",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Local tool: scan a Romanian county for accommodations without a real website, extract their photos, and generate a demo-site preview with OpenAI.",
  "engines": { "node": ">=20" },
  "scripts": {
    "start": "node server.mjs",
    "dev": "node --watch server.mjs"
  },
  "dependencies": {
    "express": "^4.19.2"
  }
}
